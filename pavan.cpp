#include <iostream>
#include <limits>
using namespace std;
int main() {
	string name;
	int num;
	cout <<"The name is :" << name << flush;
	cin >> name ;
	cin >> num ;
	cout <<"The number  is :" << num << flush;
return 0;
}